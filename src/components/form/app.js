function formSearchDateInit() {
    // const startDate = document.forms.searchDate.startDate;
    // const endDate = document.forms.searchDate.endDate;
    // const dateNow = Date.now();
    // const dateCurrent = new Date(dateNow).toISOString().slice(0, 16);
    // let dateNext = new Date(dateNow);
    // dateNext.setHours(dateNext.getHours() + 1);
    // dateNext = dateNext.toISOString().slice(0, 16);
    // startDate.value = dateCurrent;
    // endDate.value = dateNext;
}

export {formSearchDateInit};
